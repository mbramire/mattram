%a{ href: "/posts/#{post.id}", alt: "#{post.title}"}
  .gray-box.margin-top
    %img.lg-icon.pull-left{ src: (post.category.image) }
    .preview-wrapper 
      %h2
        = post.title
      %span.tiny-txt.bold-txt
        = pretty_date(post.created_at)
      %p 
        = truncate(post.body)